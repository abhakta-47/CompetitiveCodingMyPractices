#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define pb push_back

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll t = 15;
    for (int n = 1; n <= t; n++)
        cout << n << "\n";
    // for (int m = 1; m <= 1e2; m++)
    // cout << n << " " << m << "\n";
}
